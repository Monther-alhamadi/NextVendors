[pytest]
# Only collect tests from the `backend/tests` directory to avoid executing
# accidental scripts that perform network I/O at import time (e.g. tools
# under `backend/scripts` named `test_*.py`). This keeps CI/test runs hermetic.
testpaths = backend/tests
norecursedirs = backend/scripts
